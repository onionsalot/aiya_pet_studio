import TagForm from "../../components/TagForm/TagForm"

const CreateUpdateTag = () => {
    return (
        <div className="bg-white h-full overflow-y-scroll">
            <TagForm />
        </div>
    )
}

export default CreateUpdateTag
